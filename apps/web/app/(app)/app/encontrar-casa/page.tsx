import type { Metadata } from 'next';
import { FindHomeClient } from '@/modules/monetization/components/FindHomeClient';

export const metadata: Metadata = {
  title: 'Encontrar Casa',
  robots: { index: false, follow: false },
};

export default function EncontrarCasaPage() {
  return <FindHomeClient />;
}
import type { Metadata } from 'next';
import { FindHomeClient } from '@/modules/monetization/components/FindHomeClient';

export const metadata: Metadata = {
  title: 'Encontrar Casa',
  robots: { index: false, follow: false },
};

export default function EncontrarCasaPage() {
  return <FindHomeClient />;
}
