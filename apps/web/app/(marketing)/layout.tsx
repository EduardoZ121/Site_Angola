import type { ReactNode } from 'react';

/** Marketing route group — Landing and future public pages. */
export default function MarketingLayout({ children }: { children: ReactNode }) {
  return children;
}
