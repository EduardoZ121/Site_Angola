# @kuteka/web

Aplicação Next.js (App Router) — produto principal KEOS.

- `(marketing)` — foundation page; Landing após validação FASE 1
- `(auth)` / `(app)` — reservados
- `/api/health` — health check
- `/dev/ui` — catálogo do Design System
- `/dev/auth-check` — check técnico de auth (dev)

```bash
pnpm --filter @kuteka/web dev
```
