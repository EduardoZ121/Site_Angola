# Legacy deploy (Vite prototype) — disabled for KEOS.
# Production target is Vercel for apps/web (see ADR-001).
# Keep this file for historical reference only.

name: Legacy Deploy (disabled)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "Legacy Vite deploy is disabled."
          echo "Use Vercel for apps/web. Prototype lives in legacy/."
          exit 0
