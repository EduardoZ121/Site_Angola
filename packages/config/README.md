# @kuteka/config

Shared tooling presets for KEOS:

- TypeScript (`typescript/base`, `nextjs`, `react-library`)
- ESLint (`eslint/base`)
- Tailwind (`tailwind/kuteka-preset`)

See `docs/architecture/ADR-001-foundation-architecture-decisions.md`.
