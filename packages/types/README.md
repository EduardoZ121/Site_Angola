# @kuteka/types

Shared TypeScript contracts for KEOS (User, Role, Permission, Result, AppError, AuditLog).

Source of truth for cross-package types. Database-generated types may be added later under `database.ts`.
